# Docker-optimized requirements (lighter version)
# MLflow is already running in its own container, so we skip it here
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
httpx>=0.25.0
gql[httpx]>=3.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0
pandas>=2.1.0
scikit-learn>=1.3.0
numpy>=1.24.0
groq>=0.4.0
joblib>=1.3.0
prometheus-client>=0.19.0
# Note: pyarrow and mlflow removed to reduce download size for unstable connections
